import { Routes } from '@angular/router';
import { FirstScreenComponent } from './first-screen/first-screen.component';

export const routes: Routes = [
    { path: '', component: FirstScreenComponent },   // 🟢 página inicial
    // otras rutas...
];
